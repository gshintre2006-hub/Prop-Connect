# public/

Static files served at the site root.

**`logo.png`** — the PropConnect brand lockup. Used on the login screen, the
nav bar and the footer (`components/ui.jsx` → `Logo`, via `lib/logos.js`).
Export it tight-cropped (minimal surrounding whitespace). PNG with transparency
preferred. Replace this file any time to update the logo everywhere.
